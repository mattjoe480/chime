syntax = "proto3";

package ping;

message PingMsg {
  string message = 1;
}

message Pong {
  string message = 1;
}
service Ping {
  rpc Ping(PingMsg) returns (Pong);
  rpc PingStream(stream PingMsg) returns (stream Pong);
}
